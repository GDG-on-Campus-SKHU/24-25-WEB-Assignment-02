for (let a=1; a<=6; a++) {
    for (let b=1; b<=6; b++) {
        for (let c=1; c<=6; c++) {
            if (a+b+c===10) {
                console.log(`(${a}, ${b}, ${c})`);
            }
        }
    }
}
